{
  "name": "rich-editor-sample-app-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Validates @local/rich-editor as an installed package: package contract checks plus browser tests against the sample app's production build.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:package": "playwright test --project=package",
    "test:app": "playwright test --project=app",
    "validate": "npm --prefix ../sample-app run editor:update && npm --prefix ../ai-server run editor:update && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.6.0",
    "jszip": "^3.10.2",
    "typescript": "~5.9.3"
  }
}
